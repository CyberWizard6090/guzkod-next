export type Vacancy = {
  id: string;
  title: string;
  description: string;
  salary: string;
  department: string;
  isActive: boolean;
  publishedDate: string; // ISO string
  createdAt: string;
  updatedAt: string;
}